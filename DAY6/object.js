const calculator = {
  add: function (num1, num2) {
    return num1 + num2;
  },
  subtract: function (num1, num2) {
    return num1 - num2;
  },
  divice: function (num1, num2) {
    return num1 / num2;
  },
};

let result = calculator.divice(3, 4);

console.log(result);
